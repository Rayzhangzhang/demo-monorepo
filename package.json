{
  "name": "root",
  "private": true,
  "scripts": {
    "build-all": "lerna run build --stream --parallel",
    "change": "lerna changed --all",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run clean --stream --parallel",
    "publish-all": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
